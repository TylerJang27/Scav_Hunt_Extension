// TODO: TYLER ADD LOGIC FOR WHEN HUNT IS SAVED AND THAT BEGINNING IS OPENED

const sum = (a: number, b: number): number => a + b;

it("1 + 1 = 2", () => {
  expect(sum(1, 1)).toBe(2);
});

it("1 + 2 != 2", () => {
  expect(sum(1, 2)).not.toBe(2);
});

// TODO: TYLER ADD TEST FOR RESET LOGIC
